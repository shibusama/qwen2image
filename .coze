[project]
# Python项目无需node，删除node依赖

[dev]
# 数组拆分参数，不带bash嵌套
build = ["python", "-m", "pip", "install", "-r", "requirements.txt"]
# 注意：数组项不做shell展开，端口必须用 sh -c 才能读取 ${DEPLOY_RUN_PORT}
run = ["sh", "-c", "python -m uvicorn app:app --host 0.0.0.0 --port ${DEPLOY_RUN_PORT:-5000}"]

[deploy]
build = ["python", "-m", "pip", "install", "-r", "requirements.txt"]
# exec 是关键：让 uvicorn 接管 sh 进程，服务进程持续存活，避免被平台误判退出
run = ["sh", "-c", "exec python -m uvicorn app:app --host 0.0.0.0 --port ${DEPLOY_RUN_PORT:-5000}"]
